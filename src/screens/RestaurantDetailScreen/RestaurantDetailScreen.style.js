import {StyleSheet} from 'react-native';

const RestaurantDetailScreenStyle = StyleSheet.create({
  wrapperView: {
    padding: 16,
  },
  informationView: {
    padding: 16,
  },
  informationText: {
    fontSize: 17,
  },
  drawerChildView: {padding: 20},
});

export default RestaurantDetailScreenStyle;
